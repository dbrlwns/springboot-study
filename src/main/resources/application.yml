# 서버 실행시 자동으로 로딩, ex) DB 설정, 로깅 설정 정보 등

spring:

  jpa:
    # 전송 쿼리 확인
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:testdb

